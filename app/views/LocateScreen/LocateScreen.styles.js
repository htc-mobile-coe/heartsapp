import { StyleSheet } from 'react-native';

export const styles = props =>
    StyleSheet.create({
        viewWebViewContainer: { flex: 1 },
        viewWebView: { flex: 15 },
        headingContainer: { alignItems: 'center' },
    });
