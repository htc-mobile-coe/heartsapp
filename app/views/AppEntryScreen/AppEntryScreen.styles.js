import { StyleSheet } from 'react-native';

export const styles = props =>
    StyleSheet.create({
        image: {
            width: '100%',
            aspectRatio: 4,
            resizeMode: 'contain',
        },
    });
