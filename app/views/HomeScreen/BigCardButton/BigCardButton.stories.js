import React from 'react';
import { storiesOf } from '@storybook/react-native';
import '../../i18n';
import BigCardButton from './index';

storiesOf('BigCardButton', module).add('Default', () => <BigCardButton />);
