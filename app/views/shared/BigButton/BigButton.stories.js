import React from 'react';
import { storiesOf } from '@storybook/react-native';
import '../../i18n';
import BigButton from './BigButton';

storiesOf('BigButton', module).add('Default', () => <BigButton />);
